export const validateRegisterInput = (req, res, next) => {
    // Skeleton validator - to be implemented with express-validator or similar
    next();
};

export const validateLoginInput = (req, res, next) => {
    // Skeleton validator - to be implemented with express-validator or similar
    next();
};
